{
  "scripts": {
    "install": "UPDATE_REQUIREMENTS=t INSTALL=t COPY=t BUILD=t sh scripts/setup.sh",
    "start": "python -m http.server -b localhost -d ./dist",
    "restart": "sh scripts/update.sh"
  }
}
